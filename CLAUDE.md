# 新商品アイデア共有アプリ（アイデアまとめ）

## プロジェクトの目的
ぎゅう丸の新商品アイデアを店舗をまたいで社内共有し、レシピ（材料・原価・栄養）まで貯める社内ツール。
社長と現場の認識ズレを減らす（社長コメント／方針の共有）ことも狙い。**季節メニューを考える**のが主目的で、
一覧はカテゴリ（＝季節）ごとにまとまって表示されるのが既定。

## 構成（GAS ウェブアプリ＋スプレッドシート＋Drive）
- `Code.gs` … サーバー。シート読み書き・原価/栄養計算・写真/ポスターのDrive保存・あいことば検証
- `index.html` … 画面一式（CSS/JS込みの1ファイル、ビルド不要・npm不要）
- `appsscript.json` … タイムゾーン Asia/Tokyo、webapp: USER_DEPLOYING / ANYONE_ANONYMOUS
- `.clasp.json` / `README.md`

### 主要ID
- スプレッドシート: `14aEoP1eeF4LrClVLxgpiUiqQDEfA2cWde5aRYFnnFnI`
- Apps Script scriptId: `170-9kAqJqQ_-04zO7J9A8Z6c1wYcUchmmK-5QbUa1ezKkcSN72OZHrgq`
- 公開URL(/exec): `.../macros/s/AKfycbyILDDp_HQT-joelI6zTJHQddsRh0BiI9TRqHX9B7kGQRO5i-Xumb7dK2GU5UMcCc_Y/exec`
- 既存デプロイID: `AKfycbyILDDp_HQT-joelI6zTJHQddsRh0BiI9TRqHX9B7kGQRO5i-Xumb7dK2GU5UMcCc_Y`
- GitHub: https://github.com/gyumaru1978/gyumaru-idea-share （Private）

## デプロイ手順（同じURLを維持）
```bash
clasp push --force
clasp redeploy AKfycbyILDDp_HQT-joelI6zTJHQddsRh0BiI9TRqHX9B7kGQRO5i-Xumb7dK2GU5UMcCc_Y -d "変更内容"
```
現行デプロイは **@21**。素の `clasp deploy` はURLが変わるので使わない。
git は `main` に push（コミット者 `gyumaru <info@gyumaru-food.com>`、末尾に Co-Authored-By: Claude）。

### あいことば（Script Properties／ブラウザでのみ設定可）
- `APP_PASSCODE` … 全員共通のあいことば（未設定だと全操作拒否）
- `PRESIDENT_PASSCODE` … 社長モード（社長コメント投稿の本人確認。投稿ごとにサーバー再検証）
- **Claude はあいことばを入れられない** → 移行やシート削除は「ユーザーがアプリを開いた時」に走る。

## シート構成（アプリが自動生成・管理）
`アイデア`(31列) / `材料`(レシピの子) / `材料マスタ` / `店舗` / `カテゴリ` / `タグ` / `コメント` / `ポスター`。
- これら8シートは消さない（消すと空で再生成＝データ消失）。**行データの編集・削除は可**（正本はシート）。
  ただし1行目の見出しと `アイデア` の列構成は手でいじらない。
- `既読` `イベント` は運用廃止 → `cleanupLegacySheets_` が次回オープン時に自動削除。
- 列は必ず `IDEA_COL` 経由で参照。列追加は末尾＋`migrateIdeaSheet_` で移行（現行 v6＝`確認日時`追加）。

## 確定した仕様・ルール（変更時は要相談）
- **カテゴリ＝季節（棚。1つに決める）**: `春 / 夏 / 秋 / 冬 / 通年`。一覧はこれで見出し分け（既定）。
  **タグ＝ラベル（複数OK・自由）**: `プレミアム / SNS映え / お子様向け` など。「秋限定のプレミアム」＝カテゴリ秋＋タグプレミアム。
- **ステータスは2択**: `採用` / `アイデア`。表示順は「採用→アイデア」、**既定値は「アイデア」**（`STATUS_DEFAULT`）。
- **New方式（未読/既読の管理は廃止）**: アイデア行に `社長コメント日時(presAt)` と `確認日時(seenAt)` を持ち、
  `presAt > seenAt` なら New。**全員共有・端末非依存**。「見ました」チェックで seenAt 更新→全員のNewが消える。
  **社長へ返信してもNewは消さない（B案）**。カードに赤「New」ポップ、カテゴリ見出しにNew件数。API=`markSeen`。
- **サマリータイル**=`全表示 / 採用 / アイデア` の3つ（各クリックで絞り込み）。「未読」「表示中」は廃止。
- **一覧**: 既定でカテゴリごと表示。見出しは薄緑の帯＋貼り付き（ナビ下・`--ghead-top`）＋クリックで折りたたみ（`idea_collapse`保存）。
- **材料マスタ**: 材料ごとに「基準量＋単位あたり」の単価・栄養(kcal/たんぱく質/脂質/炭水化物/食塩)・アレルギー・仕入れ先。
  レシピは材料を選ぶと単位・単価が自動。栄養・アレルギーは材料から自動集計＋手動追加も可。原価は行の単価×数量。
- **ポスター**: 商品ごと＋店舗ラベル（タイトルなし）。完成イメージと同じ「枠タップ」方式で新規登録中でも追加可（保存時に登録）。DL可。
- **マスタ画面は階層メニュー**（入口→各マスタ→戻る）。並び替えは**ドラッグ**（グリップ）。店舗/カテゴリ/タグに「編集(改名)」。
- **名前(ME)は最小限**。コメントの名前は任意・端末に記憶しない。用語は「合い言葉」→「**あいことば**」で統一。

## 技術的な注意点（GAS特有・ハマりどころ）
- **テンプレートリテラル内にURLを書かない**（GASのコメント除去が `//` を誤認→全JS停止）。URLは引用符付き定数。
- **Tailwind CDN禁止**（GASのiframeで全JS停止）。CSSは手書き。
- localStorage は try/catch（iframeでブロックされ得る）。
- `setNumberFormat('@')` は必ず `setValues` の前。日付・採用月類はテキストで保持。
- **原価/栄養計算はサーバー(`calcCost_`/`autoNutrition`相当)とクライアントで二重実装**。丸めは「行ごとに丸めて合計」。
- 複数Googleアカウントにログインしていると `/exec` が「開けません」になる → シークレット or `/u/0` を案内。

## 検証方法（ローカルプレビュー）
scratchpad に `mock.js`＋`preview.html` を置き `http://localhost:8137/preview.html` で確認（GASの `google.script.run` を偽装）。
`node --check` で構文チェック、`node` でロジックの単体テスト（列往復・移行の冪等性・原価/栄養）を回す運用。

## これまでの経緯（2026-08-14〜2026-08-15）
- 08-14: GAS＋スプレッドシート構成で新規作成。GitHub・clasp・スプレッドシート/Apps Script作成、初回デプロイ。
- 08-15: タグ／店舗マスタ追加 → 社長コメント・未読既読・部門タブ → 一覧整理・カテゴリ見出し → 提案者廃止 →
  採用月/イベント廃止・ステータス2択・**材料マスタ** → ポスター（商品ごと・枠タップ・DL） →
  マスタ階層化・ドラッグ並び替え・カテゴリ編集 → 一覧カテゴリ既定・帯/折りたたみ →
  あいことば表記・編集ボタン・採用→アイデア順 → **未読既読を廃止しNew方式（共有フラグ）** → 不要シート自動削除（@21）。
- 実データ: ハンバーグ系アイデア12件（提案店舗＝ぎゅう丸ラボ）。カテゴリは秋/冬メニュー中心、タグにプレミアム等。店舗9件登録済み。

## 未確定・保留
- 次に**ユーザーがアプリを開くと** v6移行（`確認日時`列追加）と `既読`/`イベント` シート削除が走る（要確認）。
- ポスターのダウンロードが実機（GASのiframe）で確実に動くかは未検証（新規タブ/Driveの挙動しだいで要調整）。
